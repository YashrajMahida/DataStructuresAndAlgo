import DLinkList.Node;


public class DNode<E> {
	public class Node {
        E element;
        Node next;
        Node prev;
 
        public Node(E element, Node next, Node prev) {
            this.element = element;
            this.next = next;
            this.prev = prev;
        }
    }
   
    public int size() { 
    	return size(); 
    	}
     
    public boolean isEmpty() { 
    	return size() == 0; 
    	}
}
